apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: db-service
    app: postgis
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30009

  selector:
    name: db-pod
    app: postgis

