sudo: required

language: python

services:
  - docker

python:
  - '3.7'

jobs:
  include:
    - stage: Build and Test
      env:
        - SCENARIO=replications
        - SCENARIO=collations
      script:
        - ./build-test.sh
        - pushd scenario_tests/${SCENARIO}
        - ./test.sh
        - popd

